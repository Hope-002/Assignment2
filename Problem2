Write a java program to initialize three int variables a, b and c with 234, 456 and 712 and store the sum 
of the last digits of the variables.


import java.util.*;

public class Main
{
	public static void main(String[] args) 
	{
	    int a,b,c,sum=0;
	    Scanner num = new Scanner(System.in);
	    a=num.nextInt();
	    b=num.nextInt();
	    c=num.nextInt();
	    a=a%10;
	    b=b%10;
	    c=c%10;
	    sum=a+b+c;
		System.out.println(sum);
	}
}
